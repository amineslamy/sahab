برای طراحی و توسعه ماژول **«تحلیل داده»** (تحلیل متن، استخراج فرکانس موضوعی، ابر کلمات و انتیتی‌ها)، نیاز به یک معماری مشخص در دو بخش **پردازش متن (موتور تحلیلی/بک‌اند)** و **نمایش بصری (فرانت‌اند)** داریم.

با توجه به حجم گزارش‌ها و نیاز به پردازش زبان فارسی، الگوی پیشنهادی و ایده‌های تکمیلی برای پخته‌تر شدن این ماژول به شرح زیر است:

---
۱. معماری سیستم و فرآیند ارتباط (Workflow)این ماژول بر پایه یک الگوی دو‌مرحله‌ای امن پیاده‌سازی می‌شود تا ضمن حفظ ساختار یکپارچه، پرتابل (اجرا روی فلش/محلی) و «یک‌کلیکی» سامانه فعلی شما، نیازی به اجرای اجباری و همیشگی سرویس پایتون در پس‌زمینه نباشد:مرحله ۱: بررسی اتصال (Health-Check & Handshake)با ورود کاربر به صفحه «تحلیل داده»، فرانت‌اند ابتدا یک درخواست GET به آدرس محلی پایتون (مثلاً [http://127.0.0.1:8000/health](http://127.0.0.1:8000/health)) ارسال می‌کند.در صورت عدم اتصال: پیام راهنما نمایش داده می‌شود:«موتور تحلیلی پایتون فعال نیست. لطفاً به پوشه python_engine رفته و فایل analytics_server.exe را اجرا کنید. پس از مشاهده آیکون سبز/پیام آمادگی سرور، دکمه «بررسی مجدد اتصال» را فشار دهید.»در صورت اتصال موفق: وضعیت سیستم به حالت «آماده‌به‌کار» تغییر یافته و رابط کاربری اصلی داشبورد لود می‌شود.مرحله ۲: ارسال داده‌ها و پردازش (Data Stream & Processing)کاربر فیلترهای خود (تاریخ، کیس‌ها، موضوعات) را مشخص می‌کند.داده‌های متنی گزارش‌ها (شامل content و abstract) از PocketBase استخراج و به API پایتون ارسال می‌شوند.  پایتون نتایج استخراج انتیتی‌ها، ابر کلمات و جریان‌های اصلی را به‌صورت JSON به فرانت‌اند بازمی‌گرداند تا توسط کتابخانه‌های جاوااسکریپتی رندر شوند.
---

### ۱. ابزارها و کتابخانه‌های پیشنهادی

#### الف) بخش پردازش متن و داده (Python Backend)

چون پردازش زبان طبیعی (NLP) زبان فارسی نیاز به ابزارهای تخصصی دارد، پیشنهاد می‌شود پردازش‌های سنگین یا ساخت داده‌های تحلیلی توسط **پایتون** انجام شود:

1. **پردازش متن و انتیتی‌ها (NLP فارسی):**
* **Hazm یا Parsivar:** جهت آماده‌سازی متن (نرمال‌سازی، ریشه‌یابی/Stemming، حذف کلمات ایست/Stopwords مانند «از»، «به»، «در»).
* **spaCy (مدل فارسی) یا Stanza:** برای استخراج انتیتی‌ها (Named Entity Recognition - NER) مانند اسامی اشخاص، سازمان‌ها، مکان‌ها و تاریخ‌ها.


2. **استخراج جریانات اصلی (Mainstream Analysis) و الگوی موضوعات:**
* **BERTopic یا LDA (Latent Dirichlet Allocation):** جهت دسته‌بندی خودکار اسناد فیلترشده به «موضوعات داغ» (Topic Modeling) بدون نیاز به برچسب‌گذاری دستی.
* **N-grams (Tf-Idf):** جهت تشخیص ترکیبات ۲ یا ۳ کلمه‌ای پرتکرار (مثلاً «تحریم‌های اقتصادی» به جای کلمات منفرد «تحریم» و «اقتصادی»).



#### ب) بخش نمایش بصری (Frontend Libraries)

برای افزوده شدن این بخش به فرانت‌اند سامانه فعلی:

1. **ابر کلمات و انتیتی‌ها:**
* **`wordcloud2.js` یا `echarts-wordcloud`:** کتابخانه‌های سبک جاوااسکریپتی برای رندر پویا و تعاملی ابر کلمات بر روی Canvas یا SVG (با قابلیت کلیک روی هر کلمه برای فیلتر مجدد گزارش‌ها).


2. **نمودارها و جریانات زمانی (Mainstream & Trends):**
* **Chart.js یا Apache ECharts:** برای رسم نمودارهای روند خطی (Line Charts)، نمودارهای میله‌ای تجمعی و نمودار Streamgraph جهت نمایش بالا/پایین رفتن حجم یک موضوع در طول زمان.



---

### ۲. ایده‌های پیشنهادی برای توسعه ماژول «تحلیل داده»

1. **داشبورد ترکیبی ابر کلمات (Word Cloud + Entity Cloud):**
* **ابر کلمات عموم:** نمایش پرتکرارترین واژگان متن.
* **ابر موجودیت‌ها (Entities):** نمایش مجزای «اشخاص»، «سازمان‌ها» و «مکان‌ها» با رنگ‌بندی متفاوت.


2. **تحلیل روند زمانی موضوعات (Topic Trend Timeline):**
* امکان مشاهده اینکه یک کلمه یا کیس در بازه زمانی انتخابی (مثلاً ۶ ماه گذشته) چه منحنی رشدی داشته است.


3. **تحلیل شبکه ارتباطی (Network Graph):**
* نمایش گراف ارتباطی بین «کیس‌ها»، «افراد/سازمان‌ها» و «موضوعات» (مشخص شدن اینکه چه اشخاصی بیشترین ارتباط را با چه موضوعاتی داشته‌اند).


4. **موتور استخراج چکیده هوشمند و جریانات اصلی (Mainstream Digest):**
* نمایش ۳ تا ۵ تیتر یا محور اصلی اخبارِ فیلترشده بر اساس بیشترین وزن کلمات کلیدی (Tf-Idf).



---

### ۳. شیوه پیاده‌سازی متناسب با قوانین توسعه پروژه

برای اینکه بتوانیم این بخش را گام‌به‌گام و طبق ضوابط فنی پروژه‌تان پیاده‌سازی کنیم، دو مسیر جلوی روی ماست:

* **روش اول (پردازش زنده - On the fly):** ارسال گزارش‌های فیلترشده به یک API پایتون و دریافت خروجی تحلیل در لحظه.
* **روش دوم (پیش‌پردازش و کش - Pre-indexed):** استخراج کلمات کلیدی و انتیتی‌ها هنگام ثبت هر گزارش (یا در فرایند Import) و ذخیره آن‌ها در فیلدهای دیتابیس PocketBase جهت استعلام سریع در فرانت‌اند.

---

### اطلاعات مورد نیاز جهت ورود به گام بعد

لطفا طبق **قاعده شماره ۵**، مشخص کنید:

1. **نحوه اجرا:** آیا مایلید پردازش‌های NLP و ساخت خروجی‌های تحلیلی به‌صورت **API جداگانه پایتون** اجرا شود یا ترجیح می‌دهید داده‌های تحلیلی تا حد امکان در خود **PocketBase / JavaScript** پردازش و نگهداری شوند؟
2. **کد مرتبط:** زمانی که مایل بودید پیاده‌سازی فرم‌ها یا بخش‌های فرانت‌اند این ماژول را آغاز کنیم، لطفاً ابتدا فایل HTML یا JS صفحة مربوطه را ارسال فرمایید تا دقیقاً مطابق **قواعد ۱، ۳ و ۴** تغییرات روی آن اعمال شود.